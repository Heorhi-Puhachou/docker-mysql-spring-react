services:
  ui:
    container_name: react-ui-container
    image: react-ui-image
    restart: always
    networks:
      - custom-network
    ports:
      - "3001:3000"

networks:
  custom-network:
    name: custom-network
